<h1>タスク詳細画面</h1>

  <div>
    <p>タスク名: <%= @task.title %></p>
    <p>タスク詳細: <%= @task.content %></p>
    <p>終了期限: <%= @task.limit %></p>
    <p>ステータス: <%= @task.status %></p>
    <p>優先順位: <%= @task.priority %></p>

    <%= link_to "タスク一覧画面にもどる", tasks_path %>
  </div>
